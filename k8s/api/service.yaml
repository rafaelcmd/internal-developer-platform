apiVersion: v1
kind: Service
metadata:
  name: internal-developer-platform-api
  namespace: default
spec:
  type: ClusterIP
  selector:
    app: internal-developer-platform-api
  ports:
    - name: http
      port: 80
      targetPort: 8080
      protocol: TCP
